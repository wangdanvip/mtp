/**
 * 菜单列表
 * @param state
 * @returns {Array}
 */
export const menuList = state => {
    return state.menuList
};


/**
 * 项目信息
 * @param state
 * @returns {Array}
 */
export const project = state => {
  return state.project
};


